<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xoi.smvitm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/college_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/college_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PDFReader"></activity>
        <activity android:name=".splashActivity" />
        <activity android:name=".facultyRegister" />
        <activity android:name=".facProfileActivity" />
        <activity android:name=".facultyLogin" />
        <activity android:name=".selectLogin" />
        <activity android:name=".loginActivity" />
        <activity android:name=".studProfileActivity" />
        <activity android:name=".studRegisterActivity" />
        <activity android:name=".studMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>